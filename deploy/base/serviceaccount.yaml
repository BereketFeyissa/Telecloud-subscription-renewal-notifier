apiVersion: v1
kind: ServiceAccount
metadata:
  name: tele-scraper
# No Role or RoleBinding: this workload talks to the portal and to notification endpoints,
# never to the Kubernetes API (CLAUDE.md §14.8).
automountServiceAccountToken: false
