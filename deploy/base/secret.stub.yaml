# STUB ONLY - placeholder keys, no values. Never commit real credentials (CLAUDE.md §14.6).
# Supply the real Secret through SealedSecrets or External Secrets; see deploy/README.md.
apiVersion: v1
kind: Secret
metadata:
  name: tele-scraper-secrets
type: Opaque
stringData:
  PORTAL_PASSWORD: ""
  SMTP_PASSWORD: ""
  TELEGRAM_BOT_TOKEN: ""
  # Webhook URLs are credentials: the routing table references them by name via address_env.
  SLACK_WEBHOOK_OPS: ""
  DISCORD_WEBHOOK_OPS: ""
